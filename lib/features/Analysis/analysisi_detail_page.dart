import 'package:flutter/material.dart';

class AnalysisiDetailPage extends StatelessWidget {
  const AnalysisiDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SingleChildScrollView(
        child: Column(),
      ),
    );
  }
}
